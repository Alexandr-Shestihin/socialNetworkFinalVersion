let initialeState = {
   names: [
      { id: 1, name: "Andrey" },
      { id: 2, name: "Sasha" },
      { id: 3, name: "Sveta" },
   ],
};

const siteBarReducer = (state = initialeState, action) => {

   return state;
}
export default siteBarReducer;